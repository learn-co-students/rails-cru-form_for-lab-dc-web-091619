<h1> All of the Artists </h1>

  <ul>

    <% @artists.each do |artist| %>
    <li> <%= link_to artist.name, artist_path(artist) %></li>
    <% end %>

  </ul>

<button> <%= link_to "Create New Artist", new_artist_path %> </button>